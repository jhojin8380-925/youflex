<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.youflex.mapper.ReviewLikeMapper">

    <select id="existsLike" resultType="int">
        SELECT COUNT(*)
        FROM review_like
        WHERE review_id = #{reviewId} AND member_id = #{memberId}
    </select>

    <insert id="insertLike" parameterType="com.youflex.dto.ReviewLikeDTO">
        INSERT INTO review_like (review_id, member_id)
        VALUES (#{reviewId}, #{memberId})
    </insert>

    <delete id="deleteLike">
        DELETE FROM review_like
        WHERE review_id = #{reviewId} AND member_id = #{memberId}
    </delete>

    <select id="countLikes" resultType="int">
        SELECT COUNT(*)
        FROM review_like
        WHERE review_id = #{reviewId}
    </select>

    <!-- 등업 신청 조건 검증용 - 이 회원이 쓴 게시글들이 받은 좋아요를 전부 합산 -->
    <select id="countTotalLikesByAuthor" resultType="int">
        SELECT COUNT(*)
        FROM review_like rl
        JOIN review r ON r.review_id = rl.review_id
        WHERE r.member_id = #{memberId}
    </select>

</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.youflex.mapper.ReviewMapper">
	<insert id="write" parameterType="com.youflex.dto.ReviewDTO" useGeneratedKeys="true" keyProperty="reviewId">
		INSERT INTO REVIEW(
			MEMBER_ID, 
			REVIEW_PLATFORM, 
			REVIEW_RELATED, 
			REVIEW_TITLE, 
			REVIEW_CONTENT, 
			REVIEW_IMG, 
			REVIEW_RATING
		)
		VALUES(
			#{memberId}, 
			#{reviewPlatform}, 
			#{reviewRelated}, 
			#{reviewTitle}, 
			#{reviewContent}, 
			#{reviewImg}, 
			#{reviewRating}
		)
	</insert>

	<!-- 실제 테이블명은 review_genre_mapping이 아니라 review_mapping (DDL 기준) -->
	<insert id="insertReviewGenres">
    INSERT INTO review_mapping (  review_id,
        genre_category_id
    ) VALUES
    <foreach collection="genreCategoryIds" item="genreId" separator=",">
        (
            #{reviewId},
            #{genreId}
        )
    </foreach>
</insert>
</mapper>
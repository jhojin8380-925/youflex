<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.youflex.mapper.GenreCategoryMapper">

    <select id="findAll" resultType="com.youflex.dto.GenreCategoryDTO">
        SELECT *
        FROM genre_category
        ORDER BY genre_category_id
    </select>

</mapper>
